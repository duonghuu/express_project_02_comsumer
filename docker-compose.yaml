services:
  app:
    container_name: express_consumer
    network_mode: host
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    env_file:
      - .env
    restart: always